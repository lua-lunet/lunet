- **Fix**: replace deprecated `$(buildir)` with `$(builddir)` in `xmake.lua`, and remove obsolete files (#90).
- **xmake task parity** for the lint/test workflows (#62, #63, #84).
- LICENSE updated.

_Notes corrected retroactively (2026-07-25): this release originally carried v0.2.0's highlights verbatim. PAXE shipped in v0.2.0, not here._
