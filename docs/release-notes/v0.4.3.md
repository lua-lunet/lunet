### Breaking

- **Shared-dict surface renamed from `ngx_shared` to `lnt_shared`** (#113), with `lnt.store` usage standardized. Code written against the `ngx_shared` name from v0.4.0/v0.4.1 must be updated.

### Other

- **Fix**: the Postgres and MySQL drivers were missing from the release archives and are now included (#110). Archives from v0.4.2 and earlier on this line do not contain them.
- **Luacheck** set up with `.luacheckrc` and an `xmake check` task (#107).

_Notes corrected retroactively (2026-07-25): this release originally carried v0.2.0's highlights verbatim. PAXE shipped in v0.2.0, not here._
